from setuptools import setup, find_packages

setup(
    name="csmanager-api",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # requirements.txt
    ],
    python_requires=">=3.8",
)
